# Supabase Guidelines
- Always use `npx supabase` for commands related to supabase db (e.g. `npx supabase db push`).
- The project ID is `vkcoeudqeegnftkytiqd`.

# Docs
- Canonical docs: `docs/` (PRD, ADR, SPEC). Index: `docs/README.md`.
- GSD playbook: `docs/gsd/GSD.md`.

# sleepRun
This worktree is Grok's dedicated overnight UI sandbox (`/sleepRun`).
Read `docs/releases/SLEEPRUN.md` for last progress and iteration grain.
Do not mix with `production-release` or Play identity.
